# SwitchBot MCP Server - Tool Examples

This document showcases examples of the 183+ tools generated by our custom MCP generator.

## Overview

- **183 Total Tools** across **51 Device Types**
- **54 Categories** including devices, scenes, and webhooks
- **Full parameter definitions** extracted from OpenAPI schemas
- **Automatic generation** from `dist/openapi.yaml`

## Device Management

### Get All Devices
```yaml
name: getDevices
description: Get list of all devices associated with your SwitchBot account
category: Device Management
input:
  type: object
  properties: {}
  required: []
endpoint:
  method: GET
  path: /devices
```

## Smart Lock Examples

### Lock the Door
```yaml
name: SmartLock_lock
description: Lock the door - Smart Lock
category: Smart Lock
input:
  type: object
  properties:
    deviceId:
      type: string
      description: Device ID of the Smart Lock
  required:
    - deviceId
endpoint:
  method: POST
  path: /devices/{deviceId}/commands
  body:
    commandType: command
    command: lock
```

### Unlock the Door
```yaml
name: SmartLock_unlock
description: Unlock the door - Smart Lock
category: Smart Lock
input:
  type: object
  properties:
    deviceId:
      type: string
      description: Device ID of the Smart Lock
  required:
    - deviceId
endpoint:
  method: POST
  path: /devices/{deviceId}/commands
  body:
    commandType: command
    command: unlock
```

**All Lock Variants:**
- `SmartLock_lock`, `SmartLock_unlock`
- `SmartLockPro_lock`, `SmartLockPro_unlock`
- `SmartLockUltra_lock`, `SmartLockUltra_unlock`
- `SmartLockLite_lock`, `SmartLockLite_unlock`

## Color Bulb Examples

### Turn On/Off
```yaml
name: ColorBulb_turnOn
description: Turn on the device - Color Bulb
category: Color Bulb
```

### Set Brightness
```yaml
name: ColorBulb_setBrightness
description: Set brightness level - Color Bulb
input:
  properties:
    deviceId:
      type: string
      description: Device ID of the Color Bulb
    parameter:
      type: string
      description: Brightness value (0-100)
      example: "75"
  required:
    - deviceId
    - parameter
endpoint:
  method: POST
  path: /devices/{deviceId}/commands
  body:
    commandType: command
    command: setBrightness
    parameter: "{parameter}"
```

### Set Color (RGB)
```yaml
name: ColorBulb_setColor
description: Set RGB color - Color Bulb
input:
  properties:
    deviceId:
      type: string
    parameter:
      type: string
      description: RGB hex code
      example: "FF6347"  # Tomato red
  required:
    - deviceId
    - parameter
endpoint:
  method: POST
  path: /devices/{deviceId}/commands
  body:
    commandType: command
    command: setColor
    parameter: "{parameter}"
```

### Set Color Temperature
```yaml
name: ColorBulb_setColorTemperature
description: Set color temperature - Color Bulb
input:
  properties:
    parameter:
      type: string
      description: Color temperature in Kelvin
      example: "4000"  # Neutral white
```

**All Color Bulb Commands:**
- `ColorBulb_turnOn`, `ColorBulb_turnOff`
- `ColorBulb_toggle`
- `ColorBulb_setBrightness`
- `ColorBulb_setColor`
- `ColorBulb_setColorTemperature`

## Humidifier Examples

### Turn On/Off
```yaml
name: Humidifier_turnOn
description: Turn on the device - Humidifier
```

### Set Mode
```yaml
name: Humidifier_setMode
description: Set operation mode (auto, low, medium, high) - Humidifier
input:
  properties:
    deviceId:
      type: string
    parameter:
      type: string
      description: Operation mode
      enum:
        - auto
        - low
        - medium
        - high
      example: auto
  required:
    - deviceId
    - parameter
```

**All Humidifier Commands:**
- `Humidifier_turnOn`, `Humidifier_turnOff`
- `Humidifier_setMode`

## Curtain Examples

### Open/Close
```yaml
name: Curtain_turnOn
description: Open curtain - Curtain

name: Curtain_turnOff
description: Close curtain - Curtain
```

### Set Position
```yaml
name: Curtain_setPosition
description: Set curtain position - Curtain
input:
  properties:
    deviceId:
      type: string
    parameter:
      type: string
      description: Position percentage (0-100)
      example: "50"
  required:
    - deviceId
    - parameter
endpoint:
  body:
    commandType: command
    command: setPosition
    parameter: "{parameter}"
```

**All Curtain Commands:**
- `Curtain_turnOn`, `Curtain_turnOff`
- `Curtain_setPosition`

## Bot Examples

### Trigger Bot (Press)
```yaml
name: Bot_turnOn
description: Trigger the Bot (press) - Bot
input:
  properties:
    deviceId:
      type: string
      description: Device ID of the Bot
  required:
    - deviceId
endpoint:
  method: POST
  path: /devices/{deviceId}/commands
  body:
    commandType: command
    command: turnOn
```

### Trigger Bot (Retract)
```yaml
name: Bot_turnOff
description: Trigger the Bot (retract) - Bot
```

## Robot Vacuum Examples

### Start Cleaning
```yaml
name: RobotVacuumCleanerS1_start
description: Start cleaning operation - Robot Vacuum Cleaner S1
```

### Return to Dock
```yaml
name: RobotVacuumCleanerS1_dock
description: Return to charging dock - Robot Vacuum Cleaner S1
```

## Device Status Examples

### Get Smart Lock Status
```yaml
name: getSmartLockStatus
description: Get current status of Smart Lock device
category: Smart Lock
output:
  description: Current status of Smart Lock
  schema:
    lockState:
      type: string
      enum:
        - locked
        - unlocked
        - jammed
    doorState:
      type: string
      enum:
        - closed
        - opened
        - unknown
    battery:
      type: integer
      description: Battery level (0-100)
```

### Get Color Bulb Status
```yaml
name: getColorBulbStatus
output:
  schema:
    power:
      type: string
      enum: [on, off]
    brightness:
      type: integer
      description: Brightness level (1-100)
    color:
      type: string
      description: RGB hex color code
    colorTemperature:
      type: integer
      description: Color temperature in Kelvin
```

## Scene Management

### Get Scenes
```yaml
name: getScenes
description: Get list of all manual scenes
category: Scene Management
endpoint:
  method: GET
  path: /scenes
```

### Execute Scene
```yaml
name: executeScene
description: Execute a manual scene by its sceneId
input:
  properties:
    sceneId:
      type: string
      description: Unique scene identifier
  required:
    - sceneId
endpoint:
  method: POST
  path: /scenes/{sceneId}/execute
```

## Webhook Management

### Setup Webhook
```yaml
name: setupWebhook
description: Configure webhook URL to receive device status notifications
input:
  properties:
    url:
      type: string
      description: Webhook endpoint URL (must be HTTPS)
    deviceList:
      type: string
      description: Comma-separated device IDs or "ALL"
  required:
    - url
```

### Query/Update/Delete Webhook
```yaml
name: queryWebhook
description: Query current webhook configuration

name: updateWebhook
description: Update existing webhook configuration

name: deleteWebhook
description: Delete configured webhook
```

## All Device Categories

The MCP server includes tools for these device categories:

### Lights (34 tools)
- Color Bulb (7 tools)
- Strip Light, Strip Light 3 (11 tools)
- Ceiling Light, Ceiling Light Pro (10 tools)
- Floor Lamp (6 tools)
- RGBICWW variants (12 tools)
- RGBIC Neon variants (10 tools)
- Candle Warmer Lamp (4 tools)

### Smart Locks (12 tools)
- Smart Lock, Lock Pro, Lock Ultra, Lock Lite
- Each with: lock, unlock, getStatus

### Climate Control (23 tools)
- Humidifier (4 tools)
- Evaporative Humidifier variants (8 tools)
- Air Purifier VOC (5 tools)
- Smart Radiator Thermostat (3 tools)
- Circulator Fan variants (18 tools)

### Curtains & Blinds (12 tools)
- Curtain (4 tools)
- Blind Tilt (4 tools)
- Roller Shade (4 tools)

### Power Control (20 tools)
- Bot (3 tools)
- Plug, Plug Mini variants (16 tools)
- Relay Switch variants (12 tools)

### Robot Vacuums (6 tools)
- Robot Vacuum Cleaner S1, S1 Plus

### Sensors (6 tools)
- Meter, Outdoor Meter
- Presence Sensor, Water Leak Detector

### Hubs (3 tools)
- Hub, Hub 3, AI Hub

### Other Devices
- Garage Door Opener (3 tools)
- AI Art Frame, Home Climate Panel
- Virtual IR Remote
- Keypad variants (4 tools)

## Authentication

All tools use the SwitchBot API authentication:

```yaml
server:
  authentication:
    type: custom
    headers:
      - name: Authorization
        description: Access token
        required: true
      - name: sign
        description: HMAC-SHA256 signature
        required: true
      - name: t
        description: Unix timestamp in milliseconds
        required: true
      - name: nonce
        description: Random UUID
        required: true
```

## Usage with AI Assistants

This MCP configuration enables AI assistants to:

1. **Discover devices** - Get list of all SwitchBot devices
2. **Read status** - Query current state of any device
3. **Control devices** - Execute commands with proper parameters
4. **Manage scenes** - List and execute automation scenes
5. **Configure webhooks** - Set up event notifications

### Example AI Conversation:

```
User: "Turn on the living room lights and set them to 50% brightness"

AI uses:
1. getDevices - Find "Living Room Light" device ID
2. ColorBulb_turnOn - Turn on the light
3. ColorBulb_setBrightness - Set brightness to "50"

User: "Lock all doors"

AI uses:
1. getDevices - Find all Smart Lock devices
2. SmartLock_lock - Lock each found device
```

## Generation Details

Generated by: **SwitchBot Custom MCP Generator v2.0**
- Source: OpenAPI 3.1.0 specification
- Devices: 51 types with commands/status
- Total Tools: 183
- File Size: 4,299 lines of YAML
- Auto-generated: Part of `npm run build`

---

For the complete MCP server configuration, see: `dist/mcp-server.yaml`
