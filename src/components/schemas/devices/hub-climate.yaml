schemas:
  Hub3Device:
    allOf:
      - $ref: '../common.yaml#/schemas/DeviceBase'
      - type: object
        properties:
          deviceType: {type: string, enum: [Hub 3]}

  AIHubDevice:
    allOf:
      - $ref: '../common.yaml#/schemas/DeviceBase'
      - type: object
        properties:
          deviceType: {type: string, enum: [AI Hub]}

  EvaporativeHumidifierDevice:
    allOf:
      - $ref: '../common.yaml#/schemas/DeviceBase'
      - type: object
        properties:
          deviceType: {type: string, enum: [Evaporative Humidifier, Evaporative Humidifier (Auto)]}

  AirPurifierVOCDevice:
    allOf:
      - $ref: '../common.yaml#/schemas/DeviceBase'
      - type: object
        properties:
          deviceType: {type: string, enum: [Air Purifier VOC, Air Purifier Table VOC]}

  AirPurifierPM25Device:
    allOf:
      - $ref: '../common.yaml#/schemas/DeviceBase'
      - type: object
        properties:
          deviceType: {type: string, enum: [Air Purifier PM2.5, Air Purifier Table PM2.5]}

  SmartRadiatorThermostatDevice:
    allOf:
      - $ref: '../common.yaml#/schemas/DeviceBase'
      - type: object
        properties:
          deviceType: {type: string, enum: [Smart Radiator Thermostat]}
