{
  "name": "switchbot-openapi",
  "version": "1.0.0",
  "description": "Auto-updating OpenAPI specification for SwitchBot API",
  "main": "openapi.yaml",
  "type": "module",
  "scripts": {
    "bundle": "redocly bundle src/main.yaml -o openapi.yaml",
    "validate": "redocly lint openapi.yaml",
    "build": "npm run bundle && npm run validate && npm run build:docs && npm run copy:spec",
    "build:docs": "redocly build-docs openapi.yaml -o dist.html && mkdir -p dist && mv dist.html dist/index.html",
    "copy:spec": "cp openapi.yaml dist/openapi.yaml",
    "extract": "node scripts/extract-sections.js",
    "test:copilot": "node scripts/test-copilot.js",
    "deploy": "npm run build && npx vercel --prod",
    "deploy:preview": "npm run build && npx vercel"
  },
  "keywords": [
    "switchbot",
    "openapi",
    "api",
    "smart-home"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@redocly/cli": "^1.10.0",
    "yaml": "^2.3.4"
  }
}
